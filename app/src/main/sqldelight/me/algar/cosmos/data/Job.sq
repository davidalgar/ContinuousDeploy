CREATE TABLE jobs (
  _id LONG NOT NULL PRIMARY KEY AUTOINCREMENT,
  name STRING NOT NULL,
  color STRING NOT NULL
);

select_all:
SELECT *
FROM jobs
WHERE 1=1;


select_range:
SELECT *
FROM jobs
WHERE 1=1
LIMIT ?, ?;

select_by_id:
SELECT *
FROM jobs
WHERE _id=?
LIMIT 1;